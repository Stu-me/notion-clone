const express = require('express');

const router = express.Router();

router.get('/:pageId',()=>{});
router.post('/:pageId',()=>{});
router.put('/:id',()=>{});
router.delete('/:id',()=>{});
router.patch('/reorder',()=>{});

module.exports = router